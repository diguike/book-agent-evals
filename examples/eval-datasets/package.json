{
  "name": "@inferloop/eval-datasets",
  "version": "1.0.0",
  "private": true,
  "description": "ShopAgent 评测集：L1×200 + L2×100 + L3×40 + RAG×50（共 390 条）",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "tsx src/generate.ts",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@inferloop/shopagent": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
